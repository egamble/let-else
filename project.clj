(defproject egamble/let-else "1.0.2"
  :description "Macro let? with :when, :is, :else and :delay clauses in the bindings."
  :dependencies [[org.clojure/clojure "1.3.0"]
                 [useful "0.7.5-alpha4"]]
  :url "https://github.com/egamble/let-else")
