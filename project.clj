(defproject egamble/let-else "1.0.0"
  :description "Macro let? with :when and :else expressions in the bindings."
  :dependencies [[org.clojure/clojure "1.3.0"]
                 [useful "0.7.5-alpha4"]]
  :url "https://github.com/egamble/let-else")
